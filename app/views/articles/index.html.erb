<div class = "container">
<div class = "article">

<ul style="list-style: none;">
  <% @articles.each do |article| %>
    <li>
    <h2><%= link_to article.title, article_path(article) %></h2>
    <%= article.content %>
    </li>
   <% end %>
  </ul>
  </div>
</div>
